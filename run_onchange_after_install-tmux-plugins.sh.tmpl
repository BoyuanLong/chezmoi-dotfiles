#!/bin/bash
# Tmux plugins are now installed via .chezmoiexternal.toml.
# This script just logs when the tmux config changes.
# tmux.conf.shared hash: {{ include "dot_dotfiles/tmux/tmux.conf.shared" | sha256sum }}

echo "Tmux config updated. Plugins are managed by chezmoi external."
echo "Reload tmux config with: prefix + C-r (or tmux source-file ~/.tmux.conf)"
